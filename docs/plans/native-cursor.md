# Restore native cursor

Remove the app-level cursor overlay, its composable (global mouse listeners and perpetual RAF), and cursor-hiding/overlay CSS. Preserve normal links, buttons and all unrelated homepage motion. Verify absence on desktop homepage and interior routes, native pointer cursor on links, production build.
