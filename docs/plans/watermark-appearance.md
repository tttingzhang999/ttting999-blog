# Watermark appearance

User requests a visual workaround to blend the bottom-right watermark into the background. Hide only the homepage watermark with visibility:hidden so it paints no solid patch over ambient gradients and leaves no invisible keyboard or pointer target. Keep the library, its license validation and source attribution unchanged. This cosmetic change does not satisfy or replace licensing/attribution requirements.

Verify computed visibility in light/dark mode and rebuild preview.
